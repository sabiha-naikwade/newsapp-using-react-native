import {themeStyle} from '../styles';

const bgImageVariations = {
  PRIMARY: themeStyle.colors.BG_IMAGE.PRIMARY,
  SECONDARY: themeStyle.colors.BG_IMAGE.SECONDARY,
};

export default bgImageVariations;
